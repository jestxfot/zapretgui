# Local build configuration (DO NOT COMMIT your real .env)

# WSL distro name used by wsl.exe -d <name>
ZAPRET_WSL_DISTRO=Debian

# Optional: force Telegram upload via WSL+Telethon (recommended when repo is in WSL)
ZAPRET_TG_UPLOAD_WSL=1

# Optional SOCKS5 proxy (no login) for Telegram uploads (example uses Windows host IP in WSL)
ZAPRET_SOCKS5_HOST=172.21.80.1
ZAPRET_SOCKS5_PORT=10808

# Telegram MTProto API credentials (get from https://my.telegram.org)
TELEGRAM_API_ID=
TELEGRAM_API_HASH=

# GitHub token for release upload (either one works)
GITHUB_TOKEN=
GH_TOKEN=